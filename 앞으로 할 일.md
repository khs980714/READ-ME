1. 도서 목록에서 도서를 볼 때 ISBN은 굳이 없어도 될거같으므로 제거
2. AI에게 추천 받기도 필요 없으므로 제거
3. 도서 데이터를 받아올 때 NAVER API로만 하니 문제가 있는거같아. 출판사 홈페이지에서 직접 찾거나, YES24, 교보문고, 알라딘에서 각각 찾을 수 있도록 로직을 짜줘.
4. 도서 목차도 있을텐데 목차도 수집할 수 있으면 좋겠어.
5. 도서 목록에서 보고있는 형식이 현재 형식(이미지 + 도서정보)뿐만 아니라 도서 관리 탭에서 볼 수 있는 리스트 형식으로도 변경 가능했으면 좋겠어. 이를 선택할 수 있도록 `보기 방식`을 총 NNN권 의 반대편에 만들어줘. `보기 방식`에서는 `이미지` / `리스트`로 이름지어서 만들어줘.
6. 향후에는 카테고리 구분도 넣을거지만 현재에는 카테고리가 없어. 도서 목록에서 뺴줘.
7. `도서 관리` `수정`에서 `출판일`과 `페이지 수`는 필요 없어. 이거도 빼줘. (DB TABLE 포함)
8. `데이터 관리`에서 `난이도 분류 진행`탭의 난이도 분류 시작을 누르면 fastapi 서버에서 동작은 하는데 웹에서 에러가 발생해. 해결해줘.
9. `리뷰 기반` 난이도 분류에서 `도서 설명`을 기반으로 난이도 분류를 하고싶어.
10. `AI 추천 검색`에서 챗봇과의 대화를 초기화하는 버튼이 있으면 좋겠어.
11. AI의 첫 인삿말이 어색해. 앞에 공백이 많아. 이를 정리해줘. 그리고 문구도 수정하려고 해.
```
안녕하세요! 수강생님
기본적인 도서 질문, 주제 로드맵, 수준 별 도서 질문과 같은 도서와 연관된 질문을 해주세요. 
```
12. 11번에 맞게 `AI 도서 추천`의 내용도 수정해줘.
13. 채팅 치는 칸의 높이가 아이콘 크기때문에 이상해지는거같아. 제대로 맞게 수정해줘.
14. `AI 추천 검색`에서 footer가 기본적으로 보이게 만들어줘.
15. 글이 많아지면 한 페이지로 다 못보는 문제가 있어. 높이를 추가적으로 확보하기 위해 `AI 추천 검색`을 눌러서 페이지를 오면 서비스 명이 있는 공간이 `도서 목록` | `AI 추천 검색`이 있는 칸으로 가면서 현재 검정글씨인데 이를 흰 글씨로 변경했으면 좋겠어. 그러면서 기존의 `도서 목록` | `AI 추천 검색`은 해당 페이지에서 없어지게 해줘.
16. 관리자 로그인은 보통 쓸 일이 없으니 직접 `/login`을 입력하여 들어가면 로그인페이지가 보이게 만들면서 `도서 목록` | `AI 추천 검색`이 있는 해당 탭에서 `관리자 로그인`이라는게 없어지게 만들어줘. 단. 관리자로 로그인 되어있을 시 `로그아웃`은 `AI 추천 검색` 페이지에서만 안보이고 나머지에서는 보이도록 유지되게 해줘.