# ── Django ────────────────────────────────────────────────────
DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# ── 관리자 계정 (컨테이너 시작 시 자동 생성) ──────────────────
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=admin1234
DJANGO_SUPERUSER_EMAIL=admin@readme.local

# ── PostgreSQL (Docker Compose 서비스명: db) ──────────────────
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
DATABASE_URL=

# ── Naver Book Search API ─────────────────────────────────────
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# ── 알라딘 Open API (TTB) ─────────────────────────────────────
ALADIN_TTB_KEY=

# NVIDIA NIM (LLM) ───────────────────────
NVIDIA_NIM_BASE_URL=
NVIDIA_LLM_MODEL=
NVIDIA_EMBEDDING_MODEL=
NVIDIA_API_KEY=

# ── LangSmith ─────────────────────────────────────────────────
LANGCHAIN_API_KEY=
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=

# ── FastAPI 내부 URL (docker-compose 서비스명) ────────────────
MODEL_SERVER_URL=http://fastapi_server:8001

# ── 추천 임계값 ───────────────────────────────────────────────
RECOMMENDATION_THRESHOLD=0.5

# ── JWT 인증 ──────────────────────────────────────────────────
JWT_SECRET_KEY=                         # 미입력 시 DJANGO_SECRET_KEY 사용
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=15    # Access token 유효 시간(분)
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7       # Refresh token 유효 시간(일)
JWT_IDLE_TIMEOUT_MINUTES=30             # 유휴 자동 로그아웃 시간(분)